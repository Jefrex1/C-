#include <iostream>
using namespace std;

int main()
{
    int array[] = { 2, 4, 0, 4, 5, 0, 0, 4, 3, 0, 4 };

    for (int i = 0; i < sizeof(array) / sizeof(array[0]); i++) {
        if (array[i] == 0 && i != 3) {
            cout << "Index elementa z 0 znachenyam: " << i << endl;
        }
    }
}
