#include <iostream>
using namespace std;

int main() {
    int number;
    cout << "Vvedit 5-rozradne chyslo: ";
    cin >> number;

    // Перевірка на подільність на 12
    int lastDigit = number % 10;
    int numberWithoutLastDigit = number / 10;
    if ((numberWithoutLastDigit * 2 - lastDigit) % 12 == 0) {
        cout << "Число подільне на 12" << endl;
    } else {
        cout << "Число не подільне на 12" << endl;
    }

    // Перевірка на подільність на 13
    if ((numberWithoutLastDigit - lastDigit * 9) % 13 == 0) {
        cout << "Число подільне на 13" << endl;
    } else {
        cout << "Число не подільне на 13" << endl;
    }

    return 0;
}
